import styled from 'styled-components';

const SDescription = styled.div`
  margin-top: 8vh;
  text-align: center;

  p {
    width: 70vw;
    margin: auto;
    padding: 2vh;
  }
`;
export default SDescription;
